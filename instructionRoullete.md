Ты действуешь как Lead UI/UX Designer и Frontend Developer.
Твоя задача: Провести "Visual Polish" (визуальную полировку) текущего интерфейса игры Jackpot PvP.
Текущий дизайн рулетки выглядит слишком плоско. Нужно превратить его в "High-End Game UI", используя стиль "Cyber-Marine" и эффект "стеклянной трубы".

ИСХОДНЫЕ ДАННЫЕ (ЧТО УЛУЧШАЕМ):
- Цветовая гамма: Deep Teal (фон), Electric Pink (кнопка действия), Gold (деньги/победа).
- Стек: React + Tailwind CSS + Framer Motion.

СПИСОК ИЗМЕНЕНИЙ (СТРОГО СЛЕДУЙ ИМ):

1. HEADER & BANK (Смена иерархии):
- Убери заголовок "Киберарена Глубин" и ID пользователя в мелкий верхний бар.
- Сделай БАНК (Total Pot) главным героем экрана, расположи его по центру сверху.
  - Шрифт: Huge Monospace (текст 3xl или 4xl), цвет `text-amber-400`.
  - Эффект: `drop-shadow-[0_0_15px_rgba(251,191,36,0.5)]` (золотое свечение).
  - Анимация: Цифры должны "тикать" (CountUp) при изменении банка.

2. THE "GLASS PIPE" ROULETTE (Эффект трубы):
Это самое важное! Преврати рулетку в объемный элемент:
- Container: Добавь свойство `mask-image: linear-gradient(to right, transparent 0%, black 20%, black 80%, transparent 100%)`. Это создаст эффект цилиндра, где карточки плавно исчезают по краям.
- Center Marker (Курсор):
  - Сделай рамку толще (`border-2 border-amber-400`).
  - Добавь верхний треугольник-указатель.
  - Добавь внутреннее свечение `box-shadow: inset 0 0 20px rgba(251,191,36,0.2)`.
- Cards: Сделай карточки чуть прозрачнее (`bg-white/5`), а карточку, которая попадает в центр — ярче (`bg-white/10`).

3. PLAYER LIST (Compact Mode):
- Сделай список игроков более компактным, чтобы он не занимал много места. Например, используй более узкие строки или сетку.

4. BETTING CONTROLS (Ergonomics):
- Input Field: Сделай фон светлее (`bg-slate-800/50`), текст ярче. Исправь опечатку "Сумма ставк" на "Сумма (TON)".
- Quick Buttons (+1 TON, +5 TON): Сделай их не просто рамками, а полупрозрачными кнопками (`bg-teal-500/20 hover:bg-teal-500/40`).
- Кнопка "ПОСТАВИТЬ": Добавь ей легкую пульсацию (`animate-pulse`), если поле ввода заполнено и готово к отправке.

5. TYPOGRAPHY & DETAILS:
- Все цифры (Таймер, Баланс, Банк) должны быть моноширинными (`font-mono` или `font-variant-numeric: tabular-nums`), чтобы они не дергались при обновлении.
- Хеш раунда спрячь.

ТЕХНИЧЕСКОЕ ЗАДАНИЕ НА КОД:
Перепиши компоненты `GameHeader`, `Roulette`, `PlayerList` и `BetControls` с учетом этих визуальных правок. Используй Tailwind классы для теней, градиентов и масок.